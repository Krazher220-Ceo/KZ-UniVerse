# 🔧 Исправление проблемы с Vercel

## Проблема
На Vercel сайт отображал только текст без стилей и JavaScript.

## Причина
Конфигурация `next.config.js` была настроена для GitHub Pages с `basePath` и `output: 'export'`, что вызывало проблемы на Vercel.

## Решение

### 1. Обновлен `next.config.js`
Теперь конфигурация автоматически определяет платформу:
- **Vercel**: использует стандартный Next.js билд без `basePath` и `output: 'export'`
- **GitHub Pages**: использует static export с `basePath: '/KZ-UniVerse'`

### 2. Добавлен `vercel.json`
Создан файл конфигурации для Vercel.

## Что нужно сделать

1. **Отправить изменения в репозиторий:**
   ```bash
   git add next.config.js vercel.json
   git commit -m "fix: исправлена конфигурация для Vercel"
   git push origin main
   ```

2. **Vercel автоматически пересоберет проект** после push

3. **Проверить результат** на вашем Vercel домене

## Изменения в коде

### `next.config.js`
- Автоматическое определение платформы (Vercel vs GitHub Pages)
- `basePath` и `assetPrefix` применяются только для GitHub Pages
- `output: 'export'` применяется только для GitHub Pages
- На Vercel используется стандартный Next.js билд

### `vercel.json` (новый файл)
- Явная конфигурация для Vercel
- Указан framework: nextjs

## Результат
✅ Сайт должен корректно работать на Vercel с полными стилями и JavaScript
✅ GitHub Pages продолжит работать как раньше

